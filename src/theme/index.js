// src/theme/index.js

import { colors, semanticColors, colorTokens } from './colors';
import { typography, typographyTokens, fontFamilies, fontWeights } from './typography';
import { spacing, borderRadius, spacingTokens, borderRadiusTokens } from './spacing';

export const theme = {
  colors,
  semanticColors,
  colorTokens,
  typography,
  typographyTokens,
  fontFamilies,
  fontWeights,
  spacing,
  borderRadius,
  spacingTokens,
  borderRadiusTokens,
};

export default theme;